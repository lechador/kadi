/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { KADI_PROGRAM_ADDRESS } from "../programs";
import {
  getGoalStatusDecoder,
  getGoalStatusEncoder,
  type GoalStatus,
  type GoalStatusArgs,
} from "../types";

export const SET_GOAL_STATUS_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [130, 26, 65, 203, 199, 86, 226, 171],
);

export function getSetGoalStatusDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_GOAL_STATUS_DISCRIMINATOR,
  );
}

export type SetGoalStatusInstruction<
  TProgram extends string = typeof KADI_PROGRAM_ADDRESS,
  TAccountGoal extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountGoal extends string
        ? WritableAccount<TAccountGoal>
        : TAccountGoal,
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      ...TRemainingAccounts,
    ]
  >;

export type SetGoalStatusInstructionData = {
  discriminator: ReadonlyUint8Array;
  status: GoalStatus;
};

export type SetGoalStatusInstructionDataArgs = { status: GoalStatusArgs };

export function getSetGoalStatusInstructionDataEncoder(): FixedSizeEncoder<SetGoalStatusInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["status", getGoalStatusEncoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_GOAL_STATUS_DISCRIMINATOR }),
  );
}

export function getSetGoalStatusInstructionDataDecoder(): FixedSizeDecoder<SetGoalStatusInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["status", getGoalStatusDecoder()],
  ]);
}

export function getSetGoalStatusInstructionDataCodec(): FixedSizeCodec<
  SetGoalStatusInstructionDataArgs,
  SetGoalStatusInstructionData
> {
  return combineCodec(
    getSetGoalStatusInstructionDataEncoder(),
    getSetGoalStatusInstructionDataDecoder(),
  );
}

export type SetGoalStatusInput<
  TAccountGoal extends string = string,
  TAccountOwner extends string = string,
> = {
  goal: Address<TAccountGoal>;
  owner: TransactionSigner<TAccountOwner>;
  status: SetGoalStatusInstructionDataArgs["status"];
};

export function getSetGoalStatusInstruction<
  TAccountGoal extends string,
  TAccountOwner extends string,
  TProgramAddress extends Address = typeof KADI_PROGRAM_ADDRESS,
>(
  input: SetGoalStatusInput<TAccountGoal, TAccountOwner>,
  config?: { programAddress?: TProgramAddress },
): SetGoalStatusInstruction<TProgramAddress, TAccountGoal, TAccountOwner> {
  // Program address.
  const programAddress = config?.programAddress ?? KADI_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    goal: { value: input.goal ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("goal", accounts.goal),
      getAccountMeta("owner", accounts.owner),
    ],
    data: getSetGoalStatusInstructionDataEncoder().encode(
      args as SetGoalStatusInstructionDataArgs,
    ),
    programAddress,
  } as SetGoalStatusInstruction<TProgramAddress, TAccountGoal, TAccountOwner>);
}

export type ParsedSetGoalStatusInstruction<
  TProgram extends string = typeof KADI_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    goal: TAccountMetas[0];
    owner: TAccountMetas[1];
  };
  data: SetGoalStatusInstructionData;
};

export function parseSetGoalStatusInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetGoalStatusInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { goal: getNextAccount(), owner: getNextAccount() },
    data: getSetGoalStatusInstructionDataDecoder().decode(instruction.data),
  };
}
