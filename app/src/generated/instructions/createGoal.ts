/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findVaultPda } from "../pdas";
import { KADI_PROGRAM_ADDRESS } from "../programs";

export const CREATE_GOAL_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  229, 63, 42, 239, 1, 226, 219, 196,
]);

export function getCreateGoalDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CREATE_GOAL_DISCRIMINATOR);
}

export type CreateGoalInstruction<
  TProgram extends string = typeof KADI_PROGRAM_ADDRESS,
  TAccountCreator extends string | AccountMeta<string> = string,
  TAccountGoal extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCreator extends string
        ? WritableAccount<TAccountCreator>
        : TAccountCreator,
      TAccountGoal extends string
        ? WritableAccount<TAccountGoal>
        : TAccountGoal,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateGoalInstructionData = {
  discriminator: ReadonlyUint8Array;
  title: string;
  description: string;
  target: bigint;
  deadline: Option<bigint>;
};

export type CreateGoalInstructionDataArgs = {
  title: string;
  description: string;
  target: number | bigint;
  deadline: OptionOrNullable<number | bigint>;
};

export function getCreateGoalInstructionDataEncoder(): Encoder<CreateGoalInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["title", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["description", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["target", getU64Encoder()],
      ["deadline", getOptionEncoder(getI64Encoder())],
    ]),
    (value) => ({ ...value, discriminator: CREATE_GOAL_DISCRIMINATOR }),
  );
}

export function getCreateGoalInstructionDataDecoder(): Decoder<CreateGoalInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["title", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["description", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["target", getU64Decoder()],
    ["deadline", getOptionDecoder(getI64Decoder())],
  ]);
}

export function getCreateGoalInstructionDataCodec(): Codec<
  CreateGoalInstructionDataArgs,
  CreateGoalInstructionData
> {
  return combineCodec(
    getCreateGoalInstructionDataEncoder(),
    getCreateGoalInstructionDataDecoder(),
  );
}

export type CreateGoalAsyncInput<
  TAccountCreator extends string = string,
  TAccountGoal extends string = string,
  TAccountVault extends string = string,
  TAccountOwner extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  creator: Address<TAccountCreator>;
  goal: Address<TAccountGoal>;
  /**
   * Donations accumulate here rather than on the `goal` data account, so the
   * claimable balance is never entangled with that account's rent.
   */
  vault?: Address<TAccountVault>;
  owner: TransactionSigner<TAccountOwner>;
  systemProgram?: Address<TAccountSystemProgram>;
  title: CreateGoalInstructionDataArgs["title"];
  description: CreateGoalInstructionDataArgs["description"];
  target: CreateGoalInstructionDataArgs["target"];
  deadline: CreateGoalInstructionDataArgs["deadline"];
};

export async function getCreateGoalInstructionAsync<
  TAccountCreator extends string,
  TAccountGoal extends string,
  TAccountVault extends string,
  TAccountOwner extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof KADI_PROGRAM_ADDRESS,
>(
  input: CreateGoalAsyncInput<
    TAccountCreator,
    TAccountGoal,
    TAccountVault,
    TAccountOwner,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CreateGoalInstruction<
    TProgramAddress,
    TAccountCreator,
    TAccountGoal,
    TAccountVault,
    TAccountOwner,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? KADI_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    creator: { value: input.creator ?? null, isWritable: true },
    goal: { value: input.goal ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.vault.value) {
    accounts.vault.value = await findVaultPda({
      goal: getAddressFromResolvedInstructionAccount(
        "goal",
        accounts.goal.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("creator", accounts.creator),
      getAccountMeta("goal", accounts.goal),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCreateGoalInstructionDataEncoder().encode(
      args as CreateGoalInstructionDataArgs,
    ),
    programAddress,
  } as CreateGoalInstruction<
    TProgramAddress,
    TAccountCreator,
    TAccountGoal,
    TAccountVault,
    TAccountOwner,
    TAccountSystemProgram
  >);
}

export type CreateGoalInput<
  TAccountCreator extends string = string,
  TAccountGoal extends string = string,
  TAccountVault extends string = string,
  TAccountOwner extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  creator: Address<TAccountCreator>;
  goal: Address<TAccountGoal>;
  /**
   * Donations accumulate here rather than on the `goal` data account, so the
   * claimable balance is never entangled with that account's rent.
   */
  vault: Address<TAccountVault>;
  owner: TransactionSigner<TAccountOwner>;
  systemProgram?: Address<TAccountSystemProgram>;
  title: CreateGoalInstructionDataArgs["title"];
  description: CreateGoalInstructionDataArgs["description"];
  target: CreateGoalInstructionDataArgs["target"];
  deadline: CreateGoalInstructionDataArgs["deadline"];
};

export function getCreateGoalInstruction<
  TAccountCreator extends string,
  TAccountGoal extends string,
  TAccountVault extends string,
  TAccountOwner extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof KADI_PROGRAM_ADDRESS,
>(
  input: CreateGoalInput<
    TAccountCreator,
    TAccountGoal,
    TAccountVault,
    TAccountOwner,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CreateGoalInstruction<
  TProgramAddress,
  TAccountCreator,
  TAccountGoal,
  TAccountVault,
  TAccountOwner,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? KADI_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    creator: { value: input.creator ?? null, isWritable: true },
    goal: { value: input.goal ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("creator", accounts.creator),
      getAccountMeta("goal", accounts.goal),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCreateGoalInstructionDataEncoder().encode(
      args as CreateGoalInstructionDataArgs,
    ),
    programAddress,
  } as CreateGoalInstruction<
    TProgramAddress,
    TAccountCreator,
    TAccountGoal,
    TAccountVault,
    TAccountOwner,
    TAccountSystemProgram
  >);
}

export type ParsedCreateGoalInstruction<
  TProgram extends string = typeof KADI_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    creator: TAccountMetas[0];
    goal: TAccountMetas[1];
    /**
     * Donations accumulate here rather than on the `goal` data account, so the
     * claimable balance is never entangled with that account's rent.
     */
    vault: TAccountMetas[2];
    owner: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: CreateGoalInstructionData;
};

export function parseCreateGoalInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreateGoalInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      creator: getNextAccount(),
      goal: getNextAccount(),
      vault: getNextAccount(),
      owner: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateGoalInstructionDataDecoder().decode(instruction.data),
  };
}
