/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findVaultPda } from "../pdas";
import { KADI_PROGRAM_ADDRESS } from "../programs";

export const CLAIM_SOL_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  139, 113, 179, 189, 190, 30, 132, 195,
]);

export function getClaimSolDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CLAIM_SOL_DISCRIMINATOR);
}

export type ClaimSolInstruction<
  TProgram extends string = typeof KADI_PROGRAM_ADDRESS,
  TAccountGoal extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountGoal extends string
        ? WritableAccount<TAccountGoal>
        : TAccountGoal,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ClaimSolInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: Option<bigint>;
};

export type ClaimSolInstructionDataArgs = {
  amount: OptionOrNullable<number | bigint>;
};

export function getClaimSolInstructionDataEncoder(): Encoder<ClaimSolInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getOptionEncoder(getU64Encoder())],
    ]),
    (value) => ({ ...value, discriminator: CLAIM_SOL_DISCRIMINATOR }),
  );
}

export function getClaimSolInstructionDataDecoder(): Decoder<ClaimSolInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getOptionDecoder(getU64Decoder())],
  ]);
}

export function getClaimSolInstructionDataCodec(): Codec<
  ClaimSolInstructionDataArgs,
  ClaimSolInstructionData
> {
  return combineCodec(
    getClaimSolInstructionDataEncoder(),
    getClaimSolInstructionDataDecoder(),
  );
}

export type ClaimSolAsyncInput<
  TAccountGoal extends string = string,
  TAccountVault extends string = string,
  TAccountOwner extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  goal: Address<TAccountGoal>;
  vault?: Address<TAccountVault>;
  owner: TransactionSigner<TAccountOwner>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: ClaimSolInstructionDataArgs["amount"];
};

export async function getClaimSolInstructionAsync<
  TAccountGoal extends string,
  TAccountVault extends string,
  TAccountOwner extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof KADI_PROGRAM_ADDRESS,
>(
  input: ClaimSolAsyncInput<
    TAccountGoal,
    TAccountVault,
    TAccountOwner,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ClaimSolInstruction<
    TProgramAddress,
    TAccountGoal,
    TAccountVault,
    TAccountOwner,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? KADI_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    goal: { value: input.goal ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.vault.value) {
    accounts.vault.value = await findVaultPda({
      goal: getAddressFromResolvedInstructionAccount(
        "goal",
        accounts.goal.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("goal", accounts.goal),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getClaimSolInstructionDataEncoder().encode(
      args as ClaimSolInstructionDataArgs,
    ),
    programAddress,
  } as ClaimSolInstruction<
    TProgramAddress,
    TAccountGoal,
    TAccountVault,
    TAccountOwner,
    TAccountSystemProgram
  >);
}

export type ClaimSolInput<
  TAccountGoal extends string = string,
  TAccountVault extends string = string,
  TAccountOwner extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  goal: Address<TAccountGoal>;
  vault: Address<TAccountVault>;
  owner: TransactionSigner<TAccountOwner>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: ClaimSolInstructionDataArgs["amount"];
};

export function getClaimSolInstruction<
  TAccountGoal extends string,
  TAccountVault extends string,
  TAccountOwner extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof KADI_PROGRAM_ADDRESS,
>(
  input: ClaimSolInput<
    TAccountGoal,
    TAccountVault,
    TAccountOwner,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ClaimSolInstruction<
  TProgramAddress,
  TAccountGoal,
  TAccountVault,
  TAccountOwner,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? KADI_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    goal: { value: input.goal ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("goal", accounts.goal),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getClaimSolInstructionDataEncoder().encode(
      args as ClaimSolInstructionDataArgs,
    ),
    programAddress,
  } as ClaimSolInstruction<
    TProgramAddress,
    TAccountGoal,
    TAccountVault,
    TAccountOwner,
    TAccountSystemProgram
  >);
}

export type ParsedClaimSolInstruction<
  TProgram extends string = typeof KADI_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    goal: TAccountMetas[0];
    vault: TAccountMetas[1];
    owner: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: ClaimSolInstructionData;
};

export function parseClaimSolInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedClaimSolInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      goal: getNextAccount(),
      vault: getNextAccount(),
      owner: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getClaimSolInstructionDataDecoder().decode(instruction.data),
  };
}
