/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda, findSupporterPda, findVaultPda } from "../pdas";
import { KADI_PROGRAM_ADDRESS } from "../programs";

export const DONATE_SOL_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  168, 195, 198, 161, 226, 163, 222, 113,
]);

export function getDonateSolDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(DONATE_SOL_DISCRIMINATOR);
}

export type DonateSolInstruction<
  TProgram extends string = typeof KADI_PROGRAM_ADDRESS,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountGoal extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountTreasury extends string | AccountMeta<string> = string,
  TAccountSupporter extends string | AccountMeta<string> = string,
  TAccountDonor extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountGoal extends string
        ? WritableAccount<TAccountGoal>
        : TAccountGoal,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountTreasury extends string
        ? WritableAccount<TAccountTreasury>
        : TAccountTreasury,
      TAccountSupporter extends string
        ? WritableAccount<TAccountSupporter>
        : TAccountSupporter,
      TAccountDonor extends string
        ? WritableSignerAccount<TAccountDonor> &
            AccountSignerMeta<TAccountDonor>
        : TAccountDonor,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type DonateSolInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
  message: string;
};

export type DonateSolInstructionDataArgs = {
  amount: number | bigint;
  message: string;
};

export function getDonateSolInstructionDataEncoder(): Encoder<DonateSolInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
      ["message", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: DONATE_SOL_DISCRIMINATOR }),
  );
}

export function getDonateSolInstructionDataDecoder(): Decoder<DonateSolInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
    ["message", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getDonateSolInstructionDataCodec(): Codec<
  DonateSolInstructionDataArgs,
  DonateSolInstructionData
> {
  return combineCodec(
    getDonateSolInstructionDataEncoder(),
    getDonateSolInstructionDataDecoder(),
  );
}

export type DonateSolAsyncInput<
  TAccountConfig extends string = string,
  TAccountGoal extends string = string,
  TAccountVault extends string = string,
  TAccountTreasury extends string = string,
  TAccountSupporter extends string = string,
  TAccountDonor extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  config?: Address<TAccountConfig>;
  goal: Address<TAccountGoal>;
  vault?: Address<TAccountVault>;
  /** cannot redirect the protocol fee to themselves. */
  treasury: Address<TAccountTreasury>;
  supporter?: Address<TAccountSupporter>;
  donor: TransactionSigner<TAccountDonor>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: DonateSolInstructionDataArgs["amount"];
  message: DonateSolInstructionDataArgs["message"];
};

export async function getDonateSolInstructionAsync<
  TAccountConfig extends string,
  TAccountGoal extends string,
  TAccountVault extends string,
  TAccountTreasury extends string,
  TAccountSupporter extends string,
  TAccountDonor extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof KADI_PROGRAM_ADDRESS,
>(
  input: DonateSolAsyncInput<
    TAccountConfig,
    TAccountGoal,
    TAccountVault,
    TAccountTreasury,
    TAccountSupporter,
    TAccountDonor,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  DonateSolInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountGoal,
    TAccountVault,
    TAccountTreasury,
    TAccountSupporter,
    TAccountDonor,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? KADI_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
    goal: { value: input.goal ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    treasury: { value: input.treasury ?? null, isWritable: true },
    supporter: { value: input.supporter ?? null, isWritable: true },
    donor: { value: input.donor ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.vault.value) {
    accounts.vault.value = await findVaultPda({
      goal: getAddressFromResolvedInstructionAccount(
        "goal",
        accounts.goal.value,
      ),
    });
  }
  if (!accounts.supporter.value) {
    accounts.supporter.value = await findSupporterPda({
      goal: getAddressFromResolvedInstructionAccount(
        "goal",
        accounts.goal.value,
      ),
      donor: getAddressFromResolvedInstructionAccount(
        "donor",
        accounts.donor.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("goal", accounts.goal),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("treasury", accounts.treasury),
      getAccountMeta("supporter", accounts.supporter),
      getAccountMeta("donor", accounts.donor),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getDonateSolInstructionDataEncoder().encode(
      args as DonateSolInstructionDataArgs,
    ),
    programAddress,
  } as DonateSolInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountGoal,
    TAccountVault,
    TAccountTreasury,
    TAccountSupporter,
    TAccountDonor,
    TAccountSystemProgram
  >);
}

export type DonateSolInput<
  TAccountConfig extends string = string,
  TAccountGoal extends string = string,
  TAccountVault extends string = string,
  TAccountTreasury extends string = string,
  TAccountSupporter extends string = string,
  TAccountDonor extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  config: Address<TAccountConfig>;
  goal: Address<TAccountGoal>;
  vault: Address<TAccountVault>;
  /** cannot redirect the protocol fee to themselves. */
  treasury: Address<TAccountTreasury>;
  supporter: Address<TAccountSupporter>;
  donor: TransactionSigner<TAccountDonor>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: DonateSolInstructionDataArgs["amount"];
  message: DonateSolInstructionDataArgs["message"];
};

export function getDonateSolInstruction<
  TAccountConfig extends string,
  TAccountGoal extends string,
  TAccountVault extends string,
  TAccountTreasury extends string,
  TAccountSupporter extends string,
  TAccountDonor extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof KADI_PROGRAM_ADDRESS,
>(
  input: DonateSolInput<
    TAccountConfig,
    TAccountGoal,
    TAccountVault,
    TAccountTreasury,
    TAccountSupporter,
    TAccountDonor,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): DonateSolInstruction<
  TProgramAddress,
  TAccountConfig,
  TAccountGoal,
  TAccountVault,
  TAccountTreasury,
  TAccountSupporter,
  TAccountDonor,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? KADI_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
    goal: { value: input.goal ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    treasury: { value: input.treasury ?? null, isWritable: true },
    supporter: { value: input.supporter ?? null, isWritable: true },
    donor: { value: input.donor ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("goal", accounts.goal),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("treasury", accounts.treasury),
      getAccountMeta("supporter", accounts.supporter),
      getAccountMeta("donor", accounts.donor),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getDonateSolInstructionDataEncoder().encode(
      args as DonateSolInstructionDataArgs,
    ),
    programAddress,
  } as DonateSolInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountGoal,
    TAccountVault,
    TAccountTreasury,
    TAccountSupporter,
    TAccountDonor,
    TAccountSystemProgram
  >);
}

export type ParsedDonateSolInstruction<
  TProgram extends string = typeof KADI_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    config: TAccountMetas[0];
    goal: TAccountMetas[1];
    vault: TAccountMetas[2];
    /** cannot redirect the protocol fee to themselves. */
    treasury: TAccountMetas[3];
    supporter: TAccountMetas[4];
    donor: TAccountMetas[5];
    systemProgram: TAccountMetas[6];
  };
  data: DonateSolInstructionData;
};

export function parseDonateSolInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedDonateSolInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 7,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      config: getNextAccount(),
      goal: getNextAccount(),
      vault: getNextAccount(),
      treasury: getNextAccount(),
      supporter: getNextAccount(),
      donor: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getDonateSolInstructionDataDecoder().decode(instruction.data),
  };
}
